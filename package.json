{
  "name": "senior-cypress-framework",
  "scripts": {
    "cy:open": "cypress open",
    "cy:run": "cypress run",
    "cy:smoke": "cypress run --env grep=@smoke",
    "cy:regression": "cypress run --env grep=@regression",
    "test:ci": "npm run cy:smoke && npm run cy:regression"
  },
  "devDependencies": {
    "cypress": "^13.0.0",
    "@cypress/grep": "^4.0.0"
  }
}